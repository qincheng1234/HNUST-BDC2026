# v2 模型优化研究与实验路线

## 1. 当前结论

当前主模型为 `causal_factor_mixer_v2`：它对每个交易日的 300 只股票做横截面残差标准化，使用 60 个交易日的个股历史、市场路径和 8 个动态因子进行排序预测。训练标签是从下一交易日开盘到第 5 个交易日开盘的收益，采用带 5 日隔离期的 6 折滚动 OOF。

已验证的 v2 基线为：OOF Top-5 平均收益 `0.011834`、标准差 `0.036122`、稳健分数 `0.002803`、全局训练轮数 `4`。此前 v3 的收益/风险校准头使 OOF 均值降到 `0.006938`，且收益辅助头的 RankIC 为负，因此已淘汰。后续实验不得用 `data/test.csv`、`score_self` 或固定股票代码选择方案。

## 2. 近两年研究给出的可用启发

| 研究 | 可迁移结论 | 对本项目的取舍 |
| --- | --- | --- |
| [StockMixer, AAAI 2024](https://ojs.aaai.org/index.php/AAAI/article/view/28681) | 指标、时间和股票相关性可用轻量 MLP 分层建模；市场到股票与股票到市场交互可替代重型图或全注意力。 | 支持保留当前 Mixer 与 O(NK) 因子交互，不引入 300×300 图注意力。 |
| [MASTER, AAAI 2024](https://ojs.aaai.org/index.php/AAAI/article/view/27767) | 市场状态会改变特征有效性，股票相关性并非固定且可跨时间出现。 | 当前市场分支正确，但应保留多个时间尺度的市场 token，而非只把市场路径压缩为一个向量。 |
| [TimeMixer, ICLR 2024](https://openreview.net/pdf?id=7oLshfEIC2) | 细尺度波动与粗尺度趋势应分开混合，避免彼此干扰。 | 对 5 日持有期只做因果的短/中期分解，不复制其完整多预测器或长期预测设置。 |
| [TimeXer, NeurIPS 2024](https://proceedings.neurips.cc/paper_files/paper/2024/file/0113ef4642264adc2e6924a3cbbdf532-Paper-Conference.pdf) | 外生变量应和目标序列区别建模，再通过轻量交互连接。 | 将市场统计量视为外生市场路径，以小型市场 token 交互，而不把它们和个股因子等同处理。 |
| [ChronoEpilogi, NeurIPS 2024](https://proceedings.neurips.cc/paper_files/paper/2024/hash/f24e8cc1c1c06a689850ee766a7357b2-Abstract-Conference.html) | 时间序列预测中，大幅减少变量仍可能保留甚至提升泛化。 | `158+39` 不能视为默认最优；先做严格 OOF 特征组消融。 |
| [WaveDSTN, Entropy 2026](https://doi.org/10.3390/e28050548) | 在 CSI300/Alpha158 场景，将趋势与高频波动分离后再建模跨股关系，有助于降低两类信号的干扰。 | 只吸收“因果趋势—波动双路径”思想；不使用其 RWKV、GAT、预训练或外部数据。 |

这些论文均仅作为方法依据。本项目不会加载、微调或复用任何公开预训练权重。

## 3. 明确排除的方向

- 不引入公开预训练时序模型、LLM、文本/新闻模型或需要额外报备的权重。
- 不用静态行业图、股票代码 embedding、固定股票名单或固定风险覆盖规则。
- 不使用 O(N²) 的全股票注意力或 GAT；300 只股票和短样本下，计算与过拟合风险都不值得。
- 不再加入收益/下行风险校准头。v3 已用 OOF 证伪其有效性。

## 4. 实验阶段 A：先做特征组消融

目标是判断 v2 的波动是否主要来自 197 个高度相关技术指标，而不是盲目加大模型。每次实验只训练一个模型，完全使用同一数据、同一 6 折、同一随机种子和同一选股规则。

| 实验 | 输入特征 | 目的 |
| --- | --- | --- |
| A0 | 当前 `158+39` | 冻结基线。 |
| A1 | `39` | 判断较小、常见技术指标集合是否泛化更好。 |
| A2 | `158` | 判断新增 39 个指标是否是噪声来源。 |
| A3 | `158+39` 去除原始绝对价格、成交量、成交额和均线水平列，仅保留比率、波动、动量、横截面和市场特征。 | 检验残差标准化后仍可能泄露“价格层级”的变量。 |

每次只报告 OOF 指标：平均 Top-5 收益、标准差、稳健分数、最差折、胜率和训练轮数。候选必须同时满足：

1. 稳健分数高于 `0.002803`；
2. OOF 均值不低于 `0.011834`；
3. 最差折不低于 v2 的最差折；
4. 仅在以上条件成立后，才查看一次本地 `score_self` 作为留出观察。

若 A1/A2/A3 没有任何一项通过，保留 A0，不把特征消融结果强行写入最终模型。

## 5. 实验阶段 B：因果多尺度市场条件因子 Mixer

只有阶段 A 选出特征集后，才实现一个唯一的新候选 `causal_multiscale_factor_mixer_v3`。它不是 v3 风险校准头的复活，而是对时序编码的结构替换：

1. **因果双路径分解**：对 v2 的横截面残差输入，以左对齐的 2 日/5 日平均构造低频趋势，以原序列减去低频趋势构造高频波动。窗口仅使用当日及过去数据。
2. **共享轻量时间 Mixer**：趋势、波动各通过 1 个小型 MLP Mixer；共享投影层，避免两套完整编码器造成参数翻倍。
3. **多尺度市场 token**：从市场路径的最近 1、5、20 日状态构造 3 个 token；每只股票用自身表示对 token 做 O(N×3) 门控交互。市场 token 是预测时已有的全截面统计，不是未来信息。
4. **复用动态因子层**：仍使用 8 个因子 token 进行 O(NK) 股票交互；不使用静态行业信息或全图注意力。
5. **单一排序头**：维持当前 5 日排序标签与 `WeightedRankingLoss`。不再加入未经验证的多任务风险头。

该候选预期只增加约 10 万级参数，股票维度复杂度仍为 O(NK)，训练时只实例化一个模型。它将由新的模型类型、特征版本和模型目录隔离，不能覆盖 v2 产物。

## 6. 实验阶段 C：因子多样性正则（仅在阶段 B 通过后）

如果阶段 B 的 OOF 均值提高但最差折仍很差，才检查 8 个动态因子是否塌缩为相近表示。此时添加小型因子去相关正则：对归一化因子状态的 Gram 矩阵惩罚非对角项。只比较 `lambda=0` 与一个预先登记的小值，不扫描大量参数。最终排名仍只由模型预测产生。

## 7. 复现与合规门槛

- 本地开发：`DATA_MODE=local_split` 时，训练/预测只读 `data/train.csv`，`score_self.py` 独立读取 `data/test.csv`。
- 最终提交：`DATA_MODE=stock_data`，训练、OOF、预测均由赛事挂载的 `data/stock_data.csv` 在代码中划分完成。
- 每个模型产物必须记录模型类型、特征版本、股票池、训练截止日、OOF 折和选择轮数。
- `result.csv` 始终由当前训练模型对当期输入数据的排序产生；代码中不得出现最终股票代码或预置持仓。

## 8. 决策顺序

1. 先重训恢复后的 A0，确认 v2 基线可复现；若不能复现，先核对提交、数据日期、股票池和环境，不继续优化。
2. 顺序运行 A1、A2、A3；只让通过门槛的最佳特征集进入阶段 B。
3. 阶段 B 只对一个预先定义的模型候选做完整 OOF；失败即回到阶段 A 的最佳模型。
4. 阶段 C 只在 B 成功且尾部风险仍是瓶颈时执行。

